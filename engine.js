// Universe Timeline engine bootstrap.
// The simulation implementation lives in engine-core.js so it can be syntax-tested independently.
if (!window.UTEngine) console.debug('[Universe Timeline] loading simulation core…');
